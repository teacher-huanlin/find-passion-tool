# 2026-06-29

## find-passion-tool — Flask 后端 + Vercel 部署适配

- 创建 `app.py`：Flask 后端，SQLite 存储，API 路由（save/load）
- 创建 `api/index.py`：Vercel 入口，自动适配 SQLite（本地）和 PostgreSQL（生产）
- 创建 `vercel.json`、`requirements.txt`、`.gitignore`
- 修改 `templates/index.html`：添加 Jinja2 模板变量、保存并分享按钮、URL 加载结果
- 撰写 `DEPLOY.md`：包含 GitHub → Vercel → 域名绑定全流程
